services:
  xiaomi-rtsp:
    image: alexxit/go2rtc:latest
    container_name: xiaomi-rtsp
    restart: unless-stopped
    ports:
      - "1984:1984" # Web UI
      - "8554:8554" # RTSP
    environment:
      - CAMERA_IP=${CAMERA_IP}
    volumes:
      - ./config:/config
